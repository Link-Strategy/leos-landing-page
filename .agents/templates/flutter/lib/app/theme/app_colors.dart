import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const navy = Color(0xFF0D1B4B);
  static const turquoise = Color(0xFF40E0D0);
  static const forest = Color(0xFF228B22);
  static const lime = Color(0xFFADFE00);
  static const gold = Color(0xFFC8960C);
  static const energyOrange = Color(0xFFFF7E00);
  static const ctaOrange = Color(0xFFFAAF4C);

  // Surface Roles
  static const canvas = navy;
  static const background = canvas;
  static const surface = Color(0xC70D1B4B);
  static const elevated = Color(0xEB122669);
  static const soft = Color(0x8C142C78);

  // Text Roles
  static const textPrimary = Color(0xFFF7FBFF);
  static const textSecondary = Color(0xC2E3F0FF);
  static const textMuted = Color(0xB3ABCAEB);

  // Line Roles
  static const lineSubtle = Color(0x2E7BC2FF);
  static const lineStrong = Color(0xFF107EFF);

  // Semantics
  static const success = Color(0xFF3FB950);
  static const warning = Color(0xFFD29922);
  static const danger = Color(0xFFF85149);
  static const info = Color(0xFF58A6FF);
  static const muted = textMuted;
}
